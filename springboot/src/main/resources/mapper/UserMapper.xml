<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.UserMapper">

    <select id="selectByUsername" resultType="com.example.demo.entity.User">
        SELECT id, username, password, nickname, avatar, create_time, update_time
        FROM user
        WHERE username = #{username}
    </select>

    <insert id="insert" parameterType="com.example.demo.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, nickname, avatar, create_time, update_time)
        VALUES (#{username}, #{password}, #{nickname}, #{avatar}, NOW(), NOW())
    </insert>

</mapper>

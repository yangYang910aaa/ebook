<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.UserMapper">

    <select id="selectByLoginName" resultType="com.example.demo.entity.User">
        SELECT id, login_name, name, password
        FROM user
        WHERE login_name = #{loginName}
    </select>

    <insert id="insert" parameterType="com.example.demo.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (login_name, name, password)
        VALUES (#{loginName}, #{name}, #{password})
    </insert>

</mapper>

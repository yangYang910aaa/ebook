<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.ContentMapper">

    <select id="selectById" resultType="com.example.demo.entity.Content">
        SELECT id, content FROM content WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.example.demo.entity.Content">
        INSERT INTO content (id, content) VALUES (#{id}, #{content})
    </insert>

    <update id="update" parameterType="com.example.demo.entity.Content">
        UPDATE content SET content = #{content} WHERE id = #{id}
    </update>

    <delete id="deleteByIds">
        DELETE FROM content WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach>
    </delete>

    <delete id="deleteByEbookId">
        DELETE FROM content WHERE id IN (SELECT id FROM doc WHERE ebook_id = #{ebookId})
    </delete>
</mapper>
